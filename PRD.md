# PRD — Korea at a Glance

| 항목 | 내용 |
| --- | --- |
| 문서 상태 | Revised Draft v1.1 |
| 수정일 | 2026-08-20 |
| 제품명 | Korea at a Glance (가칭) |
| 제품 유형 | 외국인 여행자를 위한 반응형 한국 여행지 탐색 웹사이트 |
| 핵심 사용자 | 영어권·스페인어권의 한국 첫 방문 또는 두 번째 방문 자유여행자 |
| 지원 언어 | English(기본), Español |
| MVP 1 콘텐츠 | 6개 내부 권역, 총 30개 대표 여행지 |
| 개발 순서 | MVP 1 → MVP 2 → Phase 2 → Future / Phase 3 |

## 1. 제품 개요

**Korea at a Glance**는 한국을 방문하기 전이거나 한국에서 여행 중인 외국인 자유여행자가 주요 여행지를 지역·테마·추천 계절에 따라 빠르게 탐색할 수 있도록 돕는 웹사이트다.

첫 개발 버전인 **MVP 1**은 영어·스페인어 다국어 콘텐츠, 여행지 검색·필터, 카드 목록, 상세 정보와 외부 지도 연결에 집중한다. 내부 인터랙티브 지도와 일정 기능을 첫 버전에 함께 구현하지 않는다.

제품이 안정화되면 **MVP 2**에서 로그인 없는 14일 일정 만들기를 추가하고, 이후 **Phase 2**에서 인터랙티브 한국 지도와 개선된 일정 UX로 확장한다.

## 2. 문제 정의

한국을 처음 또는 두 번째 방문하는 외국인 여행자는 다음과 같은 어려움을 겪는다.

- 여러 지역의 대표 명소를 한 화면에서 비교하기 어렵다.
- 관광 정보가 여러 사이트에 흩어져 있고, 운영 정보가 최신인지 판단하기 어렵다.
- 번역된 장소명과 현지 표기가 달라 길찾기나 현장 의사소통이 어렵다.
- 관심사와 여행 계절에 맞는 명소만 빠르게 추리기 어렵다.
- 추천 장소를 찾은 뒤 실제 여행 일차별로 정리하려면 별도 앱이나 문서가 필요하다.

본 제품은 검수된 30개 대표 여행지부터 시작해 탐색과 현지 이동에 필요한 핵심 정보를 제공하고, 일정 기능과 지도 기능은 단계적으로 추가한다.

## 3. 타깃 사용자

### 3.1 핵심 사용자

- 영어권 여행자
- 스페인어권 여행자
- 한국을 처음 또는 두 번째 방문하는 자유여행자
- 여행 전에는 데스크톱으로 탐색하고 현지에서는 모바일로 정보를 확인하는 사용자
- 한국어에 익숙하지 않아 번역된 설명과 한국어 원문 표기가 모두 필요한 사용자

### 3.2 핵심 사용자 과업

- “한국에서 꼭 가볼 만한 장소를 지역별로 빠르게 보고 싶다.”
- “내 관심사나 방문 계절에 맞는 명소만 골라 보고 싶다.”
- “장소의 한국어 공식 명칭과 주소를 현지에서 바로 보여주고 싶다.”
- “상세 정보를 확인한 뒤 외부 지도에서 장소를 열고 싶다.”
- “나중에는 관심 있는 장소를 최대 14일 일정으로 정리하고 싶다.”

## 4. 핵심 가치

1. 한국의 주요 여행지를 한눈에 탐색한다.
2. 영어와 스페인어로 동일한 핵심 경험을 제공한다.
3. 한국어 공식 명칭과 한국어 도로명 주소를 항상 함께 제공한다.
4. 지역·테마·추천 계절을 기준으로 원하는 여행지를 빠르게 찾는다.
5. 여행지의 상세 설명, 방문 팁, 검증된 운영 정보를 확인한다.
6. 장소를 외부 지도 서비스에서 바로 연다.
7. 탐색 경험을 유지한 채 이후 사용자가 직접 여행 일정을 구성하도록 확장한다.

## 5. 제품 원칙

1. **카드 중심의 단순한 탐색**: MVP 1은 지도 없이 검색·필터·카드만으로 완전한 탐색 경험을 제공한다.
2. **번역 이상의 현지성**: 영어·스페인어 콘텐츠와 함께 한국어 원명·주소를 제공한다.
3. **검수된 소수의 추천**: 콘텐츠 양보다 30개 여행지의 대표성, 정확성, 사진과 번역 품질을 우선한다.
4. **단계적 확장**: 각 개발 단계는 독립적으로 사용 가능하고 검수 가능한 결과물을 만든다.
5. **정적 정보의 한계 공개**: 운영 정보에는 최종 검수일과 공식 정보 재확인 안내를 제공한다.
6. **개인정보 최소화**: 계정 없이 사용하고 필요한 상태만 브라우저에 저장한다.
7. **접근 가능한 기본 기능**: 복잡한 지도나 Drag & Drop 없이도 모든 핵심 작업을 완료할 수 있게 한다.

제품 UI와 시각 디자인은 **DESIGN.md**에 정의된 디자인 시스템을 따른다.

## 6. MVP 1 — 여행지 탐색

### 6.1 목표

사용자가 영어 또는 스페인어로 30개 대표 여행지를 검색·필터하고, 카드와 상세 페이지에서 방문에 필요한 정보를 확인한 뒤 외부 지도에서 장소를 열 수 있게 한다.

### 6.2 포함 기능

#### 다국어

- English를 최초 기본 언어로 사용한다.
- Español을 지원한다.
- 사용자가 언제든 영어와 스페인어를 전환할 수 있다.
- 여행지 이름, 요약, 상세 설명, 방문 팁, 선택 운영 정보와 대중교통 정보를 다국어로 제공한다.
- 한국어 공식 명칭과 한국어 도로명 주소는 선택 언어와 관계없이 항상 제공한다.
- 사용자가 선택한 언어를 브라우저에 저장한다.

#### 여행지 데이터

- 프로젝트 내부의 정적 데이터 파일로 총 30개 여행지를 제공한다.
- 기존 6개 권역 키는 내부 데이터 구조에서 유지한다.
- UI에는 영어·스페인어 사용자가 이해하기 쉬운 권역명을 표시한다.
- 지역별 명소 수를 5개씩 균등하게 맞추지 않는다.
- 외국인 관광 수요, 대표성, 접근성, 지역 다양성과 콘텐츠 품질을 고려해 권역별 개수를 정한다.
- 데이터베이스와 외부 관광 데이터 API를 사용하지 않는다.

#### 여행지 탐색

- 키워드 검색
- 지역 필터
- 테마 필터
- 추천 계절 필터
- 검색 및 필터 결과 개수
- 전체 조건 초기화
- 여행지 카드 목록

#### 여행지 카드

각 카드에는 다음 정보를 표시한다.

- 대표 이미지
- 현재 언어의 여행지 이름
- 현재 언어의 지역명
- 주요 테마
- 짧은 설명
- 추천 계절
- 상세 보기 CTA

#### 여행지 상세

상세 URL은 언어와 무관한 slug를 사용하며 새로고침과 직접 접근을 지원한다. 상세 화면에는 다음 정보를 제공한다.

- 대표 이미지
- 현재 언어의 여행지 이름
- 한국어 공식 명칭
- 현재 언어의 상세 설명
- 한국어 도로명 주소
- 지역
- 테마
- 추천 계절
- 방문 팁
- 가까운 대중교통 정보(데이터가 있는 경우)
- 운영 정보(검증된 데이터가 있는 경우)
- 최종 정보 검수일
- 공식 정보 링크(있는 경우)
- 외부 지도에서 열기 CTA

#### 외부 지도

- 웹사이트 내부에 인터랙티브 지도를 구현하지 않는다.
- 각 여행지 상세 화면에서 외부 지도 서비스를 여는 CTA를 제공한다.
- 외부 지도 연결에는 저장된 장소 URL 또는 좌표를 사용한다.

### 6.3 MVP 1에서 제외

- 한국 지도 인터랙티브 UI
- 여행지 위치 핀과 핀 미리보기
- 지도 핀과 카드의 양방향 연동
- My Trip / My Itinerary
- 여행지 일정 추가 CTA
- 일정 저장과 편집
- Drag & Drop
- 로그인, 서버 저장과 일정 공유

## 7. MVP 2 — My Trip / My Itinerary

MVP 1이 안정적으로 동작하고 30개 콘텐츠 검수가 완료된 이후 구현한다.

### 7.1 포함 기능

- 여행지 상세 또는 카드에서 My Trip에 명소 추가
- Day 1부터 최대 Day 14까지 일차 생성
- 실제 달력 날짜를 사용하지 않는 여행 일차 방식
- 여행지를 특정 Day에 추가
- 같은 여행지를 전체 일정에서 한 번만 추가
- 일정에서 여행지 삭제
- 버튼 또는 선택 UI로 여행지를 다른 Day로 이동
- 버튼 또는 선택 UI로 같은 Day 안의 여행지 순서 변경
- 브라우저 localStorage에 일정 자동 저장
- 새로고침 또는 재방문 후 일정 복원
- 손상되거나 사용할 수 없는 저장소의 오류 처리
- 로그인과 서버 없이 모든 기능 제공

### 7.2 MVP 2에서 제외

- Drag & Drop을 필수 편집 방식으로 사용
- 여러 기기 간 일정 동기화
- 일정 공유
- 실제 날짜 연결
- 이동 시간 계산과 경로 최적화
- 지도와 일정의 연동

## 8. Phase 2 — 지도 및 일정 UX 개선

### 8.1 Interactive Korea Map

- 한국 지도 표시
- 현재 결과에 해당하는 여행지 위치 핀 표시
- 검색·필터 결과와 지도 핀 동기화
- 카드 선택 시 대응 지도 핀 강조
- 지도 핀 선택 시 대응 카드 강조
- 지도 핀 미리보기
- 지도 로딩 실패 시 카드 탐색 유지

### 8.2 일정 UX 개선

- Drag & Drop을 이용한 일정 순서 변경
- 지도와 일정의 연계
- 일정 화면의 탐색, 편집 및 모바일 UX 개선

Phase 2의 지도는 기존 카드 탐색을 대체하지 않고 보조한다. 지도 공급자와 구현 방식은 Phase 2 기술 설계에서 결정한다.

## 9. Future / Phase 3

- 자동 여행 일정 추천
- 이동 시간 계산
- 여행 경로 최적화
- 실제 여행 날짜 연결
- 날씨 정보
- 실시간 또는 상세 교통 정보
- 음식점 추천
- 숙박 추천
- 여행지 즐겨찾기
- 사용자 계정
- 클라우드 일정 저장
- 일정 공유
- 사용자 리뷰 및 평점
- 분석 이벤트 고도화
- CMS
- 외부 관광 데이터 API 연동

## 10. 정보 구조

### 10.1 MVP 1

#### 홈 / 여행지 탐색

- 제품 소개와 탐색 시작 영역
- 언어 선택기
- 키워드 검색
- 지역·테마·추천 계절 필터
- 검색·필터 결과 개수
- 여행지 카드 목록
- 결과 없음 및 데이터 오류 상태

#### 여행지 상세

- slug 기반 고유 URL
- 대표 이미지와 현재 언어 콘텐츠
- 한국어 공식 명칭과 주소
- 분류 및 방문 정보
- 대중교통·운영 정보(있는 경우)
- 검수일과 정적 정보 안내
- 공식 정보 링크(있는 경우)
- 외부 지도 CTA

### 10.2 MVP 2 추가

#### My Trip / My Itinerary

- Day 1~Day 14 일차 목록
- 일차 추가·삭제
- 일차별 여행지 목록
- 여행지 날짜·순서 변경
- 개별 여행지 삭제
- 전체 일정 초기화
- 로컬 저장 상태 또는 저장 실패 안내

### 10.3 Phase 2 변경

- 홈 / 여행지 탐색에 인터랙티브 한국 지도 추가
- 검색·필터·카드·지도 상태 연동
- My Trip에 Drag & Drop과 지도 연계 추가

## 11. 기능 요구사항

### 11.1 MVP 1 — 언어

| ID | 요구사항 |
| --- | --- |
| LANG-01 | 첫 방문의 기본 언어는 English로 설정한다. |
| LANG-02 | 모든 MVP 1 화면에서 English와 Español을 전환할 수 있는 언어 선택기를 제공한다. |
| LANG-03 | 사용자가 선택한 언어는 localStorage에 저장하고 재방문 시 복원한다. |
| LANG-04 | 언어를 변경하면 현재 URL과 적용 중인 검색·필터 상태를 유지하고 UI와 콘텐츠만 전환한다. |
| LANG-05 | 여행지 상세에는 선택 언어와 관계없이 한국어 공식 명칭과 한국어 도로명 주소를 표시한다. |
| LANG-06 | 스페인어 번역이 누락되면 영어 값을 임시 표시하되, 필수 번역 누락은 MVP 1 출시 차단 결함으로 분류한다. |

### 11.2 MVP 1 — 검색과 필터

| ID | 요구사항 |
| --- | --- |
| DISC-01 | 검색은 현재 언어의 여행지 이름, 한국어 공식 명칭과 현재 언어의 권역명을 대상으로 하며 대소문자를 구분하지 않는다. |
| DISC-02 | 권역, 테마, 추천 계절 필터를 제공한다. |
| DISC-03 | 서로 다른 필터 그룹은 AND, 같은 그룹의 복수 선택은 OR 조건으로 적용한다. |
| DISC-04 | 검색 또는 필터가 변경되면 결과 개수와 여행지 카드 목록을 동시에 갱신한다. |
| DISC-05 | 검색과 필터는 함께 사용할 수 있다. |
| DISC-06 | 모든 검색어와 필터를 한 번에 제거하는 초기화 기능을 제공한다. |
| DISC-07 | 결과가 없으면 현재 언어의 안내와 필터 초기화 CTA를 표시한다. |
| DISC-08 | 내부 지도를 로드하거나 지도 상태를 관리하는 로직을 MVP 1에 포함하지 않는다. |

#### 권역 UI 명칭

내부 Region 키는 기존 값을 유지하고 UI 라벨만 다국어로 제공한다.

| 내부 키 | English | Español |
| --- | --- | --- |
| seoul-capital | Seoul & Capital Area | Seúl y área metropolitana |
| gangwon | Gangwon | Gangwon |
| chungcheong | Central Korea | Corea central |
| jeolla | Jeolla & Southwest | Jeolla y suroeste |
| gyeongsang | Busan & Southeast | Busan y sureste |
| jeju | Jeju Island | Isla de Jeju |

#### 테마 필터

1. History & Heritage / Historia y patrimonio
2. Nature & Scenic / Naturaleza y paisajes
3. Culture & Arts / Cultura y arte
4. City & Architecture / Ciudad y arquitectura
5. Coast & Island / Costa e islas
6. Family & Leisure / Familia y ocio

#### 추천 계절 필터

- Spring / Primavera
- Summer / Verano
- Autumn / Otoño
- Winter / Invierno
- All seasons / Todo el año

### 11.3 MVP 1 — 여행지 카드

| ID | 요구사항 |
| --- | --- |
| CARD-01 | 각 카드에 대표 이미지와 현재 언어의 의미 있는 alt text를 제공한다. |
| CARD-02 | 현재 언어의 여행지 이름, 지역명, 짧은 설명을 표시한다. |
| CARD-03 | 주요 테마와 추천 계절을 표시한다. |
| CARD-04 | 카드마다 상세 페이지로 이동하는 명확한 CTA를 제공한다. |
| CARD-05 | 카드 전체 또는 CTA는 키보드로 접근 가능하고 명확한 focus 상태를 제공한다. |
| CARD-06 | 이미지 로딩 실패 시 레이아웃을 유지하는 대체 영역을 표시한다. |

### 11.4 MVP 1 — 여행지 상세

| ID | 요구사항 |
| --- | --- |
| DETAIL-01 | 상세 화면은 데이터 계약에 정의된 필수 콘텐츠를 현재 언어로 표시한다. |
| DETAIL-02 | 언어와 관계없이 한국어 공식 명칭과 한국어 도로명 주소를 표시한다. |
| DETAIL-03 | nearestTransit 데이터가 있으면 현재 언어로 표시하고 없으면 해당 영역을 숨긴다. |
| DETAIL-04 | 검증된 operatingInfo가 있으면 현재 언어로 표시하고, 없으면 추정값을 표시하지 않는다. |
| DETAIL-05 | 최종 검수일과 “방문 전 공식 정보를 확인하세요”라는 의미의 안내를 표시한다. |
| DETAIL-06 | officialInfoUrl이 있으면 공식 정보 링크를 제공한다. |
| DETAIL-07 | 외부 지도 CTA는 안전한 https URL을 새 탭에서 연다. |
| DETAIL-08 | 상세 페이지는 언어와 무관한 slug URL로 직접 접근하거나 새로고침할 수 있다. |
| DETAIL-09 | 이미지 로딩 실패 시 레이아웃을 유지하는 대체 영역과 현재 언어의 alt text를 제공한다. |

### 11.5 MVP 2 — 일정 만들기

| ID | 요구사항 |
| --- | --- |
| PLAN-01 | 첫 명소를 추가하거나 빈 My Trip에 진입하면 Day 1을 생성한다. |
| PLAN-02 | 사용자는 Day 14까지 일차를 하나씩 추가할 수 있다. |
| PLAN-03 | Day 14가 있는 상태에서 일차 추가를 시도하면 최대 14일 제한을 현재 언어로 안내한다. |
| PLAN-04 | 일차가 하나면 그 일차에 바로 추가하고, 둘 이상이면 추가할 Day를 선택하게 한다. |
| PLAN-05 | 같은 여행지는 전체 일정에 한 번만 존재할 수 있다. |
| PLAN-06 | 사용자는 버튼 또는 선택 UI로 여행지를 다른 Day로 이동할 수 있다. |
| PLAN-07 | 사용자는 버튼으로 같은 Day 안에서 여행지 순서를 위아래로 변경할 수 있다. |
| PLAN-08 | 사용자는 일정에서 개별 여행지를 삭제할 수 있다. |
| PLAN-09 | 빈 Day는 즉시 삭제하고, 여행지가 있는 Day는 확인 후 포함된 여행지와 함께 삭제한다. |
| PLAN-10 | 중간 Day를 삭제하면 뒤의 Day 번호를 연속되도록 자동 재정렬한다. |
| PLAN-11 | 전체 일정 초기화는 삭제 범위를 설명하는 확인 절차 후 실행한다. |
| PLAN-12 | 일정에는 실제 달력 날짜를 배정하지 않고 Day 1~Day 14 형식만 사용한다. |

### 11.6 MVP 2 — 로컬 저장

| ID | 요구사항 |
| --- | --- |
| SAVE-01 | 일정 데이터는 localStorage에 자동 저장한다. |
| SAVE-02 | 새로고침하거나 같은 브라우저로 다시 방문하면 일차, 여행지와 표시 순서를 복원한다. |
| SAVE-03 | 저장 데이터에 스키마 버전을 포함하고 읽을 수 없는 데이터는 앱 오류 없이 안전하게 초기화한다. |
| SAVE-04 | localStorage가 차단되거나 저장에 실패하면 현재 세션에서는 기능을 유지하고, 종료 후 보존되지 않음을 안내한다. |
| SAVE-05 | 서버로 일정이나 언어 데이터를 전송하지 않는다. |

### 11.7 Phase 2 — 인터랙티브 지도

| ID | 요구사항 |
| --- | --- |
| MAP-01 | 한국 지도에 현재 검색·필터 결과의 여행지 핀을 표시한다. |
| MAP-02 | 검색·필터 결과, 카드 목록과 지도 핀의 명소 집합을 동기화한다. |
| MAP-03 | 카드 선택 시 대응 지도 핀을 강조한다. |
| MAP-04 | 지도 핀 선택 시 대응 카드를 강조하고 보이는 위치로 이동한다. |
| MAP-05 | 지도 핀 미리보기에 여행지 이름, 지역, 대표 이미지와 상세 CTA를 표시한다. |
| MAP-06 | 지도 로딩에 실패해도 검색·필터·카드 탐색을 유지한다. |

## 12. 콘텐츠 계획

### 12.1 초기 규모

- MVP 1 출시 목표는 총 30개 대표 여행지다.
- 내부적으로 6개 권역 체계를 유지하되 권역별 5개 균등 배분을 요구하지 않는다.
- 외국인 관광 수요, 한국 대표성, 접근성, 지역 및 테마 다양성, 사용 가능한 이미지와 검증 가능한 정보의 품질을 기준으로 선정한다.
- 특정 권역에 더 많은 명소를 배정할 수 있지만 6개 권역이 모두 탐색 가능해야 한다.

### 12.2 콘텐츠 작성 원칙

- 영어와 스페인어는 직역보다 여행자가 이해하기 쉬운 자연스러운 표현을 사용한다.
- 스페인어는 특정 국가 방언에 치우치지 않는 중립적 표현을 기본으로 한다.
- 역사·문화 설명에는 필요한 맥락을 제공하되 과도한 전문 용어를 피한다.
- 공식 한국어 명칭과 한국어 도로명 주소를 원문 그대로 보존한다.
- 운영 시간, 휴무일, 입장료처럼 변경 가능한 정보는 검증된 경우에만 작성한다.
- 모든 여행지에 최종 검수일을 기록하고 가능한 경우 공식 정보 링크를 제공한다.
- 사진 사용 권한과 출처를 확인하고 영어·스페인어 alt text를 작성한다.
- 대중교통 정보는 검증 가능한 경우에만 nearestTransit에 작성한다.

## 13. 데이터 계약

MVP 1의 여행지 데이터는 프로젝트 내부의 정적 데이터 파일로 관리한다. 아래 모델은 기존 구조를 유지하며 MVP 2와 Phase 2 확장을 위한 필드도 제거하지 않는다.

```ts
type Locale = "en" | "es";

type Region =
  | "seoul-capital"
  | "gangwon"
  | "chungcheong"
  | "jeolla"
  | "gyeongsang"
  | "jeju";

type Theme =
  | "history-heritage"
  | "nature-scenic"
  | "culture-arts"
  | "city-architecture"
  | "coast-island"
  | "family-leisure";

type Season = "spring" | "summer" | "autumn" | "winter" | "all-season";

interface LocalizedText {
  en: string;
  es: string;
}

interface Destination {
  id: string;
  slug: string;
  name: LocalizedText;
  koreanName: string;
  summary: LocalizedText;
  description: LocalizedText;
  koreanAddress: string;
  region: Region;
  themes: Theme[];
  seasons: Season[];
  coordinates: {
    latitude: number;
    longitude: number;
  };
  images: Array<{
    src: string;
    alt: LocalizedText;
    credit?: string;
  }>;
  nearestTransit?: LocalizedText;
  operatingInfo: LocalizedText | null;
  visitTips: LocalizedText[];
  externalMapUrl: string;
  officialInfoUrl?: string;
  lastVerifiedAt: string; // ISO 8601 date
}

// MVP 2에서 활성화한다.
interface ItineraryItem {
  destinationId: Destination["id"];
  position: number;
}

interface ItineraryDay {
  dayNumber: number; // 1~14
  items: ItineraryItem[];
}

interface StoredAppState {
  schemaVersion: 1;
  preferredLocale: Locale;
  itinerary?: {
    days: ItineraryDay[];
    updatedAt: string;
  };
}
```

### 13.1 데이터 유효성 규칙

- id와 slug는 전체 데이터에서 고유하고 공개 후 변경하지 않는다.
- 위도는 -90~90, 경도는 -180~180 범위여야 한다.
- 각 여행지에는 하나의 권역과 하나 이상의 테마·추천 계절이 있어야 한다.
- 영어·스페인어 필수 필드는 공백일 수 없다.
- 대표 이미지는 최소 1개이며 영어·스페인어 alt text를 모두 제공한다.
- externalMapUrl은 MVP 1 필수이며 유효한 https URL이어야 한다.
- officialInfoUrl은 선택 필드지만 존재하면 유효한 https URL이어야 한다.
- nearestTransit과 operatingInfo는 검증된 데이터가 있을 때만 제공한다.
- lastVerifiedAt은 유효한 ISO 8601 날짜여야 한다.
- MVP 2 일정의 dayNumber는 1부터 중간 누락 없이 최대 14까지 이어져야 한다.
- MVP 2 일정에서 하나의 destinationId는 전체 일정에 한 번만 존재할 수 있다.
- 같은 Day의 position은 0부터 중복 없이 연속되어야 한다.

## 14. 오류 및 빈 상태

| 단계 | 상황 | 동작 |
| --- | --- | --- |
| MVP 1 | 검색·필터 결과 없음 | 현재 언어의 안내, 조건 요약과 필터 초기화 CTA를 표시한다. |
| MVP 1 | 정적 데이터 로딩 실패 | 재시도 안내를 표시하고 손상된 카드나 빈 상세 화면을 렌더링하지 않는다. |
| MVP 1 | 이미지 로딩 실패 | 레이아웃 크기를 유지하는 대체 영역과 alt text를 표시한다. |
| MVP 1 | 스페인어 번역 누락 | 영어로 임시 대체하고 출시 전 검수에서는 차단한다. |
| MVP 1 | nearestTransit 없음 | 대중교통 영역을 숨긴다. |
| MVP 1 | operatingInfo 없음 | 추정값을 표시하지 않고 공식 정보 확인 안내를 제공한다. |
| MVP 1 | 외부 지도 URL 오류 | CTA를 비활성화하고 한국어 주소를 복사할 수 있게 한다. |
| MVP 2 | localStorage 저장 실패 | 세션 내 편집을 유지하고 종료 후 저장되지 않음을 안내한다. |
| MVP 2 | 손상된 저장 데이터 | 앱을 중단하지 않고 일정만 안전하게 초기화한 뒤 안내한다. |
| MVP 2 | 중복 여행지 추가 | 기존 Day를 알려주고 해당 일정 항목으로 이동하는 CTA를 제공한다. |
| MVP 2 | 15번째 Day 추가 | 현재 언어로 최대 14일 제한을 안내한다. |
| MVP 2 | 내용이 있는 Day 삭제 | 포함 여행지가 함께 제거됨을 설명하고 확인 또는 취소를 제공한다. |
| MVP 2 | 전체 일정 초기화 | 복구할 수 없음을 설명하는 확인 절차를 제공한다. |
| Phase 2 | 내부 지도 로딩 실패 | 지도 오류 안내를 표시하고 카드 탐색은 유지한다. |

## 15. 접근성

### 15.1 MVP에서 우선 보장

- 문서 구조와 컨트롤에 semantic HTML을 사용한다.
- 모든 핵심 탐색과 CTA를 키보드로 조작할 수 있게 한다.
- 모든 인터랙티브 요소에 명확한 focus 상태를 제공한다.
- 콘텐츠 이미지에 현재 언어의 의미 있는 alt text를 제공한다.
- 텍스트와 주요 UI 요소에 충분한 색상 대비를 제공한다.
- 주요 터치 영역은 최소 44×44px로 제공한다.
- 모바일에서 콘텐츠와 기능이 손실되지 않는 반응형 디자인을 제공한다.
- 오류와 상태 메시지를 텍스트로 명확하게 전달한다.
- 색상만으로 선택이나 오류 상태를 구분하지 않는다.

MVP 2의 일정 이동은 Drag & Drop에 의존하지 않고 버튼 또는 선택 UI로 수행할 수 있어야 한다.

### 15.2 출시 전 품질 검수

WCAG 2.2 AA 전체 검증, 스크린 리더 점검, 확대·고대비 환경과 고급 접근성 테스트는 각 버전의 출시 전 품질 검수 단계에서 수행한다.

## 16. 성능

### 16.1 MVP에서 우선 보장

- 첫 화면에 필요하지 않은 이미지는 lazy loading한다.
- 이미지의 width·height 또는 aspect ratio를 미리 확보해 레이아웃 이동을 줄인다.
- 화면 크기에 맞는 반응형 이미지와 적절한 압축 포맷을 사용한다.
- 검색과 필터는 30개 정적 데이터에서 즉시 반응하도록 클라이언트에서 처리한다.
- 내부 인터랙티브 지도 라이브러리를 MVP 1 번들에 포함하지 않는다.
- 모바일 네트워크에서도 핵심 텍스트와 카드 탐색이 이미지보다 우선 사용 가능해야 한다.

### 16.2 출시 전 품질 검수

대표 모바일 환경에서 Core Web Vitals와 주요 페이지 로딩을 측정한다. 목표치는 LCP 2.5초 이하, CLS 0.1 이하, INP 200ms 이하로 두되, MVP 개발 중에는 핵심 흐름 완성과 명백한 성능 회귀 방지를 우선한다.

## 17. 개인정보 및 보안

- MVP 1은 계정, 위치 권한, 이름, 이메일을 수집하지 않는다.
- 언어 선택은 브라우저 localStorage에만 저장한다.
- MVP 2 일정은 브라우저 localStorage에만 저장하며 서버로 전송하지 않는다.
- 브라우저 데이터 삭제 시 설정과 일정이 사라질 수 있음을 일정 기능에 안내한다.
- 외부 URL은 허용된 https 형식인지 검증한다.
- 새 탭으로 여는 외부 링크에는 noopener와 noreferrer 정책을 적용한다.
- 분석 도구 도입 시 원문 검색어와 일정 내용을 수집하지 않는다.
- 쿠키 또는 외부 분석 도구를 도입하기 전 관련 동의 요구사항을 별도로 검토한다.

## 18. 출시 승인 기준

### 18.1 MVP 1

#### 기능

- [ ] 첫 방문은 English로 표시된다.
- [ ] English와 Español 전환이 모든 MVP 1 화면에서 동작한다.
- [ ] 언어 변경 후에도 현재 상세 URL과 검색·필터 상태가 유지된다.
- [ ] 새로고침 후 사용자가 선택한 언어가 복원된다.
- [ ] 검색과 지역·테마·계절 필터를 단독 및 조합해 사용할 수 있다.
- [ ] 검색·필터 결과 개수와 카드 목록이 항상 일치한다.
- [ ] 결과 없음 상태와 전체 초기화가 두 언어로 동작한다.
- [ ] 각 카드에 필수 정보와 상세 CTA가 표시된다.
- [ ] 30개 상세 페이지가 고유 slug URL로 직접 접근 가능하다.
- [ ] 상세 페이지에 한국어 공식 명칭과 주소가 항상 표시된다.
- [ ] 선택 대중교통·운영·공식 정보 필드는 데이터 유무에 따라 올바르게 표시되거나 숨겨진다.
- [ ] 모든 외부 지도 CTA가 올바른 장소를 안전하게 연다.
- [ ] MVP 1 코드와 UI에 내부 지도 및 일정 기능이 포함되지 않는다.

#### 콘텐츠

- [ ] 전체 30개 여행지가 제공되고 6개 내부 권역이 모두 포함된다.
- [ ] 권역별 개수는 수요와 대표성을 기준으로 결정되고 균등 배분을 강제하지 않는다.
- [ ] 모든 필수 영어·스페인어 콘텐츠가 작성되고 검수된다.
- [ ] 공식 한국어 명칭과 도로명 주소가 정확하다.
- [ ] 좌표와 외부 지도 링크가 실제 장소와 일치한다.
- [ ] 모든 여행지에 최종 검수일이 기록된다.
- [ ] 모든 이미지의 사용 권한, 출처와 언어별 alt text를 확인한다.

#### 품질

- [ ] 모바일, 태블릿, 데스크톱에서 검색·필터·카드·상세 화면을 사용할 수 있다.
- [ ] semantic HTML, 키보드 탐색, focus, alt text, 색상 대비와 주요 터치 영역을 검수한다.
- [ ] 이미지 lazy loading과 이미지 영역 사전 확보가 적용된다.
- [ ] 출시 전 WCAG 2.2 AA와 Core Web Vitals 품질 검수를 수행한다.

### 18.2 MVP 2

- [ ] Day 1부터 Day 14까지 일정을 생성할 수 있다.
- [ ] 15번째 Day 추가가 차단되고 현재 언어로 안내된다.
- [ ] 같은 여행지는 전체 일정에 한 번만 추가된다.
- [ ] 버튼 또는 선택 UI로 날짜와 순서를 변경할 수 있다.
- [ ] 개별 여행지와 Day를 삭제할 수 있다.
- [ ] 중간 Day 삭제 후 번호가 연속되도록 재정렬된다.
- [ ] 새로고침 후 일차, 여행지와 순서가 복원된다.
- [ ] localStorage를 사용할 수 없는 환경에서도 세션 내 편집이 가능하다.
- [ ] 로그인, 서버 저장, Drag & Drop 없이 핵심 일정 흐름을 완료할 수 있다.

### 18.3 Phase 2

- [ ] 지도 핀과 현재 검색·필터 결과가 일치한다.
- [ ] 카드와 지도 핀 선택 상태가 양방향으로 연동된다.
- [ ] 지도 실패 시에도 카드 탐색이 유지된다.
- [ ] Drag & Drop을 사용할 수 없는 경우에도 기존 버튼 기반 일정 편집이 유지된다.

## 19. 위험 및 대응

| 위험 | 영향 | 대응 |
| --- | --- | --- |
| 첫 버전 범위가 다시 커짐 | 개발 지연과 미완성 기능 증가 | MVP 1 출시 승인 기준에 없는 기능은 구현하지 않고 후속 단계 백로그로 이동 |
| 운영 정보가 오래됨 | 현지 방문 실패와 신뢰 저하 | 검수일 표시, 공식 링크 제공, 출시 전 전수 검수와 정기 검수 주기 수립 |
| 번역 품질 불균형 | 정보 오해와 신뢰 저하 | 공통 용어집, 영어·스페인어 검수 담당자 지정, 필수 번역 누락을 출시 차단 |
| 30개 콘텐츠 준비 지연 | 기능 개발은 끝났지만 출시 불가 | 데이터 스키마와 샘플 데이터로 UI를 먼저 개발하고 콘텐츠 검수를 병렬 진행 |
| 이미지 권리 또는 용량 문제 | 법적 위험과 성능 저하 | 출처 기록, 사용 권한 확인, 업로드 전 크기·포맷 기준 적용 |
| 외부 지도 링크 오류 | 현지 이동 실패 | 30개 링크 전수 수동 검수와 주소 복사 대체 기능 제공 |
| 브라우저 데이터 삭제 | MVP 2 일정 소실 | localStorage 한계를 명확히 안내하고 클라우드 저장은 Future로 유지 |
| Phase 2 지도가 기본 탐색을 방해 | 접근성·성능 저하 | 카드 탐색을 항상 독립적으로 유지하고 지도는 점진적으로 로드 |

## 20. 후속 결정

개발 전에 다음 사항을 확정한다.

1. 가칭 Korea at a Glance를 유지할지와 최종 로고·브랜드명
2. 출시할 30개 여행지 목록과 권역별 실제 배분
3. 영어·스페인어 콘텐츠 작성자, 번역 검수자와 승인 절차
4. 사진 출처, 라이선스, 파일 크기와 권장 비율
5. 정적 데이터 파일의 형식(JSON 또는 TypeScript 등), 위치와 검증 방식
6. 외부 지도 서비스와 externalMapUrl 생성·검수 방식
7. 운영 정보의 “검증됨” 기준과 정기 재검수 주기
8. 라우팅, 호스팅과 배포 방식
9. MVP 1에 기본 페이지 조회·오류 분석을 포함할지 여부
10. MVP 2 착수 조건과 Phase 2 지도 공급자 선정 시점

